spring:
    jpa:
        database-platform: org.hibernate.dialect.H2Dialect
        hibernate:
            ddl-auto: create-drop
    datasource:
        driverClassName: org.h2.Driver
        url:  jdbc:h2:mem:db;MODE=MSSQLServer;CASE_INSENSITIVE_IDENTIFIERS=TRUE;DATABASE_TO_UPPER=FALSE
        username: sa
        password:
